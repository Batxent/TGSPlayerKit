name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  swiftpm:
    name: SwiftPM Tests
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Swift version
        run: swift --version
      - name: Test
        run: swift test
